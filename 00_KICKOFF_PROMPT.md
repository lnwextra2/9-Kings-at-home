# KICKOFF — วางข้อความนี้เป็นข้อความแรกใน Claude Code

> **วิธีใช้:** แตกไฟล์ทั้งหมดลงในโฟลเดอร์โปรเจคเปล่า (`CLAUDE.md` ต้องอยู่ที่ root)
> เปิด Claude Code ในโฟลเดอร์นั้น แล้ววางข้อความข้างล่างนี้

---

เราจะสร้างเกม **Kingdom Battle** — roguelite deckbuilder + auto-battler 2D ด้วย **Godot 4.x (GDScript)**

เอกสารอยู่ในโปรเจคแล้ว อ่านให้ครบก่อนเริ่ม:
- `CLAUDE.md` — กฎเขียนโค้ด + workflow
- `docs/01_GAME_DESIGN.md` — เกมเล่นยังไง
- `docs/02_ARCHITECTURE.md` — โค้ดจัดโครงยังไง
- `docs/03_MILESTONES.md` — ลำดับงาน

**บริบทของผม:** เคยทำเกม 3D ด้วย Godot มาแล้ว 2D เป็นของใหม่ ช่วยอธิบายท่าที่เป็น 2D-specific หน่อย แต่ไม่ต้องอธิบายพื้นฐาน Godot ทั่วไป

**เกมนี้ไม่ใช่ tower-defense ทั่วไป** จุดที่คนมักเข้าใจผิด:
> **กระดาน 5×5 คือกระดานวางแผน ไม่ใช่สนามรบ**
> การ์ดบนกระดานจะถูก "แปล" เป็นยูนิตในสนามรบตอนจบเทิร์น
> การ์ดทหาร 1 ใบ Lv3 = ทหาร 9 ตัวในสนาม และการ์ดไม่ตายตอนสู้ — เทิร์นหน้าสปอว์นใหม่เต็มจำนวน

**ก่อนเริ่มเขียนโค้ด** ขอให้ยืนยันความเข้าใจ 3 ข้อนี้ก่อนสั้นๆ:
1. ความต่างระหว่างกระดานวางแผนกับสนามรบ
2. ทำไม `core/` ห้ามรู้จักชื่อการ์ด (และ EffectData / hook script ต่างกันยังไง)
3. ทำไมห้ามสร้าง Node ต่อ 1 ยูนิต

**แล้วเริ่ม M0:** วางโครงโปรเจคให้รันได้ — โครงโฟลเดอร์, `project.godot`, `CardData` schema, `GameState` เปล่า, autoload `Game`, และ `board_view` ที่วาดกริด 5×5 เห็นช่องล็อก/ช่องว่างต่างกัน

ยังไม่ต้องมี combat ยังไม่ต้องมีการ์ดเยอะ — แค่กดรันแล้วเห็นกระดาน

ถ้ามีอะไรต้องถามก่อนเริ่ม ถามมาได้เลย
